# Together AI Duel App

A Streamlit-based duel simulator using Together AI language models.

## To run

```
pip install -r requirements.txt
streamlit run app.py
```

Replace `"YOUR_TOGETHER_API_KEY"` in `helper.py` with your actual API key.